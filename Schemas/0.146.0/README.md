# Codex CLI schema snapshot 0.146.0

Generated from the reviewed Codex CLI 0.146.0 contract:

```sh
codex app-server generate-json-schema --experimental --out Schemas/0.146.0
```

The checked-in files provide a stable fixture and review baseline. Unknown protocol data is preserved by `JSONValue`. Run `Scripts/check-schema-drift.sh` before release.
